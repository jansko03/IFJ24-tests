const ifj = @import("ifj24.zig");



pub fn main() void{
    const a: i32 = 5;
    const b: i32 = 2;
    const c: i32 = 10;
    const d: i32 = 7;

    const result = ((a + b) * (c - 3) / 2) + (d * 3) - ((c + a) / b);
    ifj.write(result);
}